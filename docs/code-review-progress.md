# Code review / dev-profile audit — progress log

Autonomous overnight run. Started 2026-09-11. Continues the walk begun in
`docs/dev-profile-coverage.md` (kept as-is for the layers it already covers: `cmd/service/main.go`,
`internal/app/`, `internal/config/` — all **DONE**, see that file for the line-by-line tables).
This file picks up every remaining layer plus the matreshka migration, and is what the user
reviews in the morning.

Legend (same as the original doc): **L** linter-enforced · **C** custom-linter candidate ·
**F** language/build-forced · **P** deliberate preference, recorded not enforced.

Status key per section: ⏳ queued · 🔄 running · ✅ done (verified green) · ⚠️ done with open
questions for the user.

---

## 0. Matreshka v1.0.94 → v1.0.100 migration + verv `gen_servers.go` fix — ⚠️ DONE, NEEDS YOUR REVIEW

### What was asked, and is done

- `~/verv/verv`: `gen_servers.go:21` `"Error parsing servers to config"` → lowercase, fixed,
  committed (`c11a0f9`), pushed to `master`. `version/version.yaml` bumped `v0.0.48` → `v0.0.49`.
  `go install`ed locally — `verv --version` now reports `v0.0.49`, confirmed on `PATH`.
- `~/verv/Velez`: `go.mod` bumped `go.vervstack.ru/matreshka` `v1.0.94` → `v1.0.100` (pulled
  `go.redsock.ru/rerrors` `v0.0.7` → `v0.0.8` along with it, expected). `moti.yaml` repinned to
  `github.com/vervstack/Matreshka@v1.0.100`, `moti g` re-run. All ~15 files that imported the
  deleted `go.vervstack.ru/matreshka/pkg/matreshka_api` repointed to Velez's own generated copy at
  `internal/api/clients/matreshka/pkg/matreshka_api/`, with the proto rename drift applied
  (`MatreshkaBeAPI*` → `MatreshkaApi*`, `ApiVersion_*` → `Version_*`, `StoreConfig_*` →
  `SaveConfig_*`). `verv project tidy` regenerated `internal/config/` — `load.go` is now genuinely
  generated (fresh `sync.Once`-guarded `Init`/`Load`, the wrap-message bug is gone at the source),
  plus new `internal/config/skeleton.go`/`skeleton.yaml`. `internal/config/load_test.go` (the
  hand-written test of what should be generated code, flagged as item J previously) is **deleted**.
  `go build ./...` clean, `golangci-lint run ./...` **0 issues** (after mechanical fixes below),
  `go test ./...` — see below.

### ⚠️ Scope creep you should look at before committing

Running `verv project tidy` with a `verv` binary that turned out to be far ahead of what Velez was
last tidied with (`~/verv/verv`'s HEAD had accumulated commits unrelated to the matreshka fix,
adding a whole cookie/CSRF-auth scaffolding feature to its project template) pulled a **second,
unrelated feature** into the diff, beyond what was asked:

- **New `internal/middleware/` files**: `cookie_names.go`, `cookie_annotator.go`,
  `cookie_response.go`, `csrf_interceptor.go`, `request_scheme_annotator.go` — a full
  cookie-based-auth + double-submit-CSRF scaffold (`CookieToMetadataAnnotator`,
  `CookieForwardResponseOption`, `GrpcCSRFInterceptor`, `RequestSchemeAnnotator`). Well-written,
  well-commented, clearly a verv template (not hand-invented) — same category as `custom.go`:
  user-editable, no `DO NOT EDIT` header.
  - `RequestSchemeAnnotator` and `CookieForwardResponseOption` **are** wired into
    `internal/transport/gateway_mux.go` (new file from the same regen) — so cookie-forwarding is
    live.
  - `GrpcCSRFInterceptor` / `NewCSRFInterceptor` are defined but **not called anywhere** — CSRF
    enforcement itself is inert, dead code for now. Nothing currently sets the
    `x-set-cookie-*` metadata keys either (no login/refresh handler in this codebase uses them
    yet), so in practice **none of this scaffold does anything at runtime today** — it's inert
    until a real auth flow wires into it.
- **`internal/transport.NewServerManager`'s signature changed** to require a CORS origins list.
  The regen produced `c.serverManager, err = transport.NewServerManager(a.Ctx, a.MASTER,
  transport.AllowAllOrigins)` in `custom.go`, with a TODO comment explaining there's no
  `allowed_origins` config wired up yet (that env var is only auto-registered by `verv init`, not
  `tidy`) and that `AllowAllOrigins` is verv's documented escape hatch preserving the old
  no-CORS-restriction behavior. This does **not** change runtime behavior (still wide open), just
  makes the previously-implicit "no CORS check" explicit and named.
- `api/grpc/tasks.proto`: one comment-only fix (`matreshka_api.ConfigTypePrefix` →
  `matreshka_api.ConfigType`, matching the actual rename) — trivial, not a behavior change.
- New `.verv/vervonomicon.yaml` (`name: go.vervstack.ru/Velez`) — verv's own project-identity
  metadata file for the newer CLI version, harmless.
- All of `internal/api/server/velez_api/*.pb.go` show as modified — re-generated by the newer
  `moti`/protoc toolchain pulled in transitively; diffed as **generated-code churn** (formatting/
  comment/version-marker changes from the toolchain bump), not a manual touch. Worth a `git diff`
  skim before committing but not treated as a concern here.

**I did not revert any of this.** Reverting selectively risks leaving `internal/config/` and
`internal/app/`'s generated files in a state that doesn't match what `verv project tidy` actually
produces together — the safer move was to leave the full, consistent regen output as one diff, run
it through mechanical lint cleanup, and flag it clearly here rather than commit anything or guess
at what you'd want kept. **Nothing is committed** — you have full control to `git checkout` the
middleware/CSRF files and the `AllowAllOrigins` line specifically if you don't want this feature
yet, independent of keeping the matreshka + config-generation fix.

### Mechanical fixes applied on top (no-brainer, verified)

The newly-generated middleware/transport files had real lint debt (grouper needs one `const`
block per file, `nlreturn`/`wsl_v5` blank-line placement, `wrapcheck` on two unwrapped external
errors) — fixed directly, matching the rest of the codebase's established patterns:
`internal/middleware/cookie_names.go` (merged 8 separate top-level consts into one block),
`cookie_annotator.go` + `cookie_response.go` + `csrf_interceptor.go` (const grouping,
`rerrors.Wrap` on `req.Cookie(...)` and `rand.Read(...)` errors, blank-line placement),
`internal/transport/http.go` (5 blank-line insertions). `golangci-lint run ./...` → **0 issues**
after these fixes.

### Verification status

- `go build ./...` — clean.
- `golangci-lint run ./...` — 0 issues.
- `go test ./...` (unit) — one real failure found and fixed (below), rest green.
- `go test ./tests/e2e/...` — green. One real regression found and fixed, one flake
  investigated and confirmed unrelated. Details:

**Fixed: `Test_ListEnvironments_EmptyLocalStateConfig`.** The regenerated `internal/config/load.go`
now stacks a compiled-in `internal/config/skeleton.yaml` under the config file (new mechanism from
the tidied matreshka option-based `ReadConfig` API), and that skeleton defaults `environments` to
`[prod]`. The test's fixture config has no `environments` key at all, so it went from a true empty
value to `[prod]` — `ListEnvironments` then returned 2 rows instead of 1. Confirmed this is a
test-fixture-only effect, not a production behavior change: both `config/config.yaml` and
`config/dev.yaml` already set `environments` explicitly (pre-existing). Fixed by adding
`WithEnvironments(nil)` to that one test's `NewEnvironment(...)` call, using the override mechanism
the harness already has for exactly this — see `tests/e2e/suite_control_plane_test.go`.

**Investigated, not a regression: transient `Test_Vpn` failure under full-suite parallel run.**
A full `go test ./tests/e2e/...` run (after the fix above) reported overall `FAIL` with zero
`--- FAIL:`-labeled subtests except one `Error Trace` at `shared_headscale.go:128`:
`headscale /api/v1/user returned 401 Unauthorized`, from `awaitAPIReady`'s 30s readiness poll.
Nothing in this migration's diff touches `shared_headscale.go` or the headscale client/fixture
code (`git diff --stat` confirms only trivial import-grouping edits in
`internal/clients/cluster_clients/headscale/*`). Reran `Test_Vpn` in isolation
(`go test ./tests/e2e/... -run Test_Vpn -v -count=1`): passed cleanly in 8s
(`Test_ConnectService_LaunchesSidecar` 5.25s, `Test_NamespaceCrud` 0.39s). Conclusion: this is
CPU/Docker-daemon contention from the full parallel suite spinning many containers at once
outrunning the fixture's 30s `headscaleAPIReadyTimeout` — the same class of issue this project's
own CLAUDE.md already documents for the `WithMatreshka()` singleton under parallel load, just for
the headscale fixture instead. Not fixed (a timeout bump is a judgment call on a pre-existing test,
out of scope for a migration PR) — flagging here in case you want to bump
`headscaleAPIReadyTimeout` (currently 30s) for CI stability.
- `graphify update .` — done.

Not committed — full diff left for your review.

---

## 1. `pkg/web/Velez-UI/` (frontend) — ✅ DONE

Ran in parallel with §0. Audited against `40-frontend-react.md`, `41-frontend-style.md`,
`42-css.md`, `43-frontend-testing.md`. This app already carries its own
`pkg/web/Velez-UI/CLAUDE.md` + `eslint.config.js`/`stylelint.config.js` from a prior audit pass
(commit `d608e442`, 2026-07-27) that wired most profile rules as baselined `warn` rules with
tracked violation counts. This pass re-verified that work, checked for drift since July, fixed
what was safe unsupervised, and left everything else recorded below. `bun run build`,
`bun run lint` (0 errors), `bun run test` (135/135) all verified clean after the fixes.

### React structure (`40-frontend-react.md`)

| Concern | Class | Detail |
|---|---|---|
| Default-exported function components | L | `func-style: [warn, declaration]` — 6 live violations (baseline was 5) |
| `Props` interface, destructured | P | Followed by convention, no violations sampled |
| `handleX` declarations | L | Same `func-style` rule |
| One component per file | L (partial) | `react/no-multi-comp: warn` — 41 violations / 18 files (baseline 44/18, slightly improved) |
| Max-3-level JSX nesting | L | `react/jsx-max-depth: [warn, {max:3}]` — 59 violations / 15 files (baseline 56 — **drifted up**) |
| Layering: pages/widgets/segments/components | L | `import-x/no-restricted-paths` — zero live violations in those four dirs |
| Layering: processes/model/app | **C**, gap | Project's own CLAUDE.md defines 7 layers (adds Processes/Model/App) but lint zones only cover the top 4. Real violation found: `processes/mappings/smerds.ts` imports `ServiceCardData` from `@/components/service/ServiceCard` (upward). Not fixed — needs a design call (move the type to `model/`) plus a composition-root exception for `app/router/*` before the lint zones can tighten without breaking the app shell itself. |
| Dialog upward-import exception | L | Verified zero dialogs import a page |
| Colocation | P | Followed — `pages/service/widgets/`, `dialogs/PluginManageDialog/plugins/`, etc. |
| State: Zustand global / `useQuery` reusable / `useState` local | P | Verified in sampled files, no violations |
| Async: promise chains over try/catch | **C**, fixed | 2 real violations (`processes/ApiService.ts` `call()`, `processes/api/tasks.ts` `WatchTaskStream()`) wrapped a single `await` in try/catch purely to translate the error — converted to `.catch()` chains. Other try/catch sites catch *synchronous* exceptions (`localStorage`, `new URL`, `atob`) — rule doesn't apply, left as-is. |
| Memoization: `useMemo` for loop/sort/filter | **C**, not fixed | Systemic gap — only 14 `useMemo` sites app-wide; unmemoized filter/map/sort in render bodies of `HomePage.tsx`, `ControlPlanePage.tsx` (×3), `TopBar.tsx` (always-mounted), `KanbanBoard.tsx`, `ServiceListView.tsx`, `NodeHealthList.tsx`. No linter can catch this; fixing is a ~7-file sweep, left recorded not swept. |
| One confirmation primitive, no native dialogs | **C**, not fixed | One live `window.confirm(...)` in `pages/service/widgets/DeployMenu.tsx` (baseline comment had a stale pre-move path). Project has no generic `ConfirmDialog` primitive yet, only per-entity ones — real fix is feature work, out of audit scope. |
| Stray debug output | — fixed | Removed a bare `console.log(res)` in `processes/api/service.ts`; removed a `console.log`-only no-op handler body in `DeploymentsPage.tsx` (params prefixed `_`, still unimplemented placeholder). |
| Dead lint suppressions | — fixed | Removed 2 stale `eslint-disable-next-line react-hooks/exhaustive-deps` in `DeployMenu.tsx` — that rule is globally off, eslint itself flagged them unused. |

### Style (`41-frontend-style.md`)

| Rule | Status | Detail |
|---|---|---|
| `no-restricted-syntax` (raw button/input, template-literal className, native alert/confirm, div w/o className, zIndex, >6-prop destructure) | L | 84 hits / 49 files (baseline ~89 — improved) |
| Max line length 120 | L | 35 violations / 17 files (baseline 27/10 — **drifted up**) |
| Import tier order | L | 120 violations / 69 files (baseline 148 — improved) |
| `no-explicit-any` | L | error severity via `tseslint recommended` — zero live hits outside generated `.pb.ts` |
| gRPC clients only in `processes/` | L | 43 files now (baseline doc said 26 — real drift, worth a baseline-comment refresh, not a rule change) |
| Double quotes everywhere | **gap, not fixed** | No `quotes` rule configured; codebase genuinely mixed (478 single- vs 436 double-quote imports). Enabling today breaks lint across ~half the repo — recorded, not swept. |
| No trailing semicolons | **gap, not fixed** | Same drift — 135/167 files still semicolon+single-quote style, 32 already no-semi+double-quote. |
| No space inside import braces | **gap, not fixed** | Unconfigured; 408 no-space vs 40 with-space — smaller backlog, plausible near-term `--fix` candidate but still a multi-file sweep. |

### CSS (`42-css.md`)

| Rule | Status | Detail |
|---|---|---|
| `z-index` forbidden | L | error severity, verified **zero** live occurrences |
| `!important` forbidden | L | error severity, verified **zero** live occurrences |
| Units are `rem` | L | warn, 355 baseline hits, not re-verified file-by-file (too large) |
| PascalCase classes / `{Name}Container` root | P/L | 5 small leaf-atom modules skip the `Container` suffix (plausible deliberate exception for single-element leaves) |
| Real pre-existing stylelint **error** | — fixed | `comment-empty-line-before` (never explicitly downgraded, unlike every other base-config rule in this file) was failing at error severity — 8 real errors in `PipelineFlowLoader.module.css`'s animation-choreography comments, breaking `bun run lint`'s exit code. Added `ignore: ['after-comment', 'stylelint-commands']` matching the sibling `rule-empty-line-before` treatment 3 lines below. `bun run lint` now exits 0. |
| Mobile detection / capped-container scroll | not audited | out of budget this pass, no violations surfaced incidentally |

### Testing (`43-frontend-testing.md`, `50-testing-posture.md`)

Colocated tests, module-level `vi.mock`, text/role assertions — all verified clean (29 test files
sampled). Zero occurrences of the banned `as unknown as T` — and zero need yet for
`as Partial<T> as T` either, since the mock style here builds typed literals/whole-module mocks.
`tests/e2e` itself out of scope for this pass (covered separately, §9).

### Files changed

`processes/ApiService.ts`, `processes/api/tasks.ts` (try/catch → promise chain),
`processes/api/service.ts` (removed stray `console.log`), `pages/deployments/DeploymentsPage.tsx`
(removed console.log-only handler body), `pages/service/widgets/DeployMenu.tsx` (removed 2 dead
eslint-disable directives), `stylelint.config.js` (fixed the real `comment-empty-line-before`
error).

### Proposed rule changes (needs approval)

None — every gap found was either this repo's own lint-config coverage, or a real code violation
of a correctly-stated global rule. One non-rule observation: the project's own
`pkg/web/Velez-UI/CLAUDE.md` layer table has no composition-root exception for `app/router/*`,
unlike the global profile which has no "app" tier at all — worth the user adding one before
tightening `no-restricted-paths` to cover `processes/`/`model`/`app/`.

---

## 2. `internal/domain/` + `internal/utils/` + `internal/middleware/` + `internal/patterns/` + `internal/clients/` — ✅ DONE

`go build ./internal/...` clean; `golangci-lint run` on all five trees — 0 issues; `go test`
green package-by-package (including real-Docker-backed suites in `docker/dockerutils`,
`container_runtime`, `hardware`). `tests/e2e` not run (out of scope).

**Fixed** (11 files): a real logic bug in `internal/utils/common/less.go` (`Less[T]` always
returned `b`, silently defeating the Docker list-limit cap — only caller was
`dockerutils/list_containers.go`; added `less_test.go` regression test); an HTTP body leak in
`headscale/ns_list.go` (non-200 path never closed the response body) and a redundant double-close
in `ns_create.go`; a crash risk in `docker/dockerutils/images.go` (`RepoTags[0]` indexed with no
length guard — untagged/dangling images ship an empty slice) plus a nil-hole slice bug in the same
function; a wrong error-library import in `pull_image.go` (`github.com/pkg/errors` →
`go.redsock.ru/rerrors`, matching the sibling file); dead code removed (`client_key_read.go`'s
unused exported type, `parser/restart.go`'s ineffectual assignment); raw `t.Errorf`/`t.Fatalf`
converted to `require` in `cluster_clients/state/pg_test.go` and
`node_clients/hardware/hardware_manager_test.go` (careful use of `require.Same`/`require.NotSame`
where the original test checked pointer identity, not value equality, so a naive `require.Equal`
would have silently weakened the assertion). One near-miss self-correction: two bare `//nolint` in
`local_state/state.go` were initially removed as apparently-dead, `golangci-lint` then surfaced the
`mnd` findings they'd been suppressing — restored as `//nolint:mnd`, net a no-op vs. pre-audit
state, just self-documenting now.

**Flagged, not fixed** (architectural/judgment calls, standing override says flag not decide):
- 🔴 **`internal/domain/` layering violation, pervasive, not a one-off.** 6 of ~30 files import
  proto or generated-storage types directly: `configuration.go` (`go.redsock.ru/evon`,
  `matreshka_api`, `velez_api`), `deployments.go` (**imports a sqlc-generated storage type**,
  `deployments_queries` — a repository-layer type living inside `domain`), `pg_instance.go`,
  `pipeline_request.go`, `plugins.go`, `service.go` (all `velez_api`, plus `go.redsock.ru/toolbox`
  in `service.go`). This is `31-go-layering.md`'s "domain imports only stdlib + primitive-type
  libs" rule, violated broadly enough (6/30 files) that it reads as the codebase's actual working
  convention rather than a handful of mistakes. Same finding independently cross-referenced by the
  service/storage audit below.
- `headscale/api_key_issue.go`'s commented-out `s.collectGarbage(ctx)` call + TODO, and
  `headscale/node_register.go`'s entire function body commented out to a `_ = req; return nil`
  stub — both look deliberate, not obviously wrong.
- `node_clients/ports/port_manager.go`'s `freePorts map[uint32]bool` actually stores "is locked"
  (`true` = locked), the inverse of its name — correct and consistently used, naming-only, but a
  likely trap for the next person to touch this file.

**Proposed rule changes:**
1. **Enable `bodyclose` in `.golangci.yaml`.** Found one real, reachable leak it exists to catch
   (`ns_list.go`) plus a related double-close it wouldn't catch but that turned up auditing the
   same pattern by hand.
2. **31-go-layering.md's domain-purity rule** reads as aspirational, not descriptive, here — needs
   an explicit decision: relax it for this project (document domain as "value objects, may
   reference generated proto/storage types for now"), or schedule the untangling as tracked debt.
3. No `depguard` currently bans third-party error libraries (deleted as dead+stale in the earlier
   pass) — `pull_image.go`'s `github.com/pkg/errors` usage sat unnoticed until this audit found it
   by hand. Worth a real `depguard` re-add if that discipline matters.

Full detail (per-package tables, every file read): `docs/code-review-progress-part-domain-clients.md`.

---

## 3. `internal/cluster/` + `internal/jobs/` — ✅ DONE

`go build ./...` clean; `golangci-lint run ./internal/cluster/... ./internal/jobs/...` 0 issues;
`go vet` clean; `go test` all green (several `internal/cluster` subpackages have no test files —
by design, per `50-testing-posture.md`, their behavior is exercised by `tests/e2e`).

**Fixed** (13 items, all message/typo/no-behavior-risk or verified-safe): message casing, stray
wording, trailing spaces, an empty `rerrors.Wrap(err, "")`, a string-concatenated log message
(→ typed field), a computed-and-discarded `strconv.Itoa` dead statement, a misleadingly-aliased
`errors "go.redsock.ru/rerrors"` import (6 call sites renamed) — across `cluster_state/master.go`,
`configuration/{connect_external,deploy_local}.go`, `env/{is_container,volume}.go`,
`env/containerinfo/container.go`, `env/container_service_task/task.go`,
`service_discovery/launch.go`, `vpnconnect/{steps,runner}.go`. Plus two real bugs:
- **`enable_statefull.go` logged a freshly-generated Postgres password in plaintext** —
  `log.Info().Str("pwd", j.pwd.GetUserPwd())`, sitting directly under a `// TODO Think about it
  really gud` that most likely meant exactly this. Dropped the password field, logs the (fixed,
  non-secret) username instead.
- **`create_smerd.go` and `upgrade_smerd.go`** both called
  `rerrors.Wrap(err, "error creating network: %s", n.GetNetworkName())` — `rerrors.Wrap` is not
  printf-style (confirmed against `wrapper.go`); both string args get silently joined with `"; "`
  instead of formatted, producing a garbled message. Fixed to `rerrors.Wrapf` at both (copy-pasted)
  sites; swept the rest of both trees for the same pattern, no further instances.
- `enable_statefull.go` also had a stray stdlib `errors.As` alongside otherwise-consistent
  `rerrors.*` calls (swapped to `rerrors.As`), and `assemble_config.go`'s production code imported
  `github.com/stretchr/testify/assert/yaml` (a test library) for YAML unmarshalling — swapped to
  the already-direct-dependency `gopkg.in/yaml.v3` (identical signature).

**Flagged, not fixed:**
- `configuration_synchronizer/synchronizer.go` is entirely dead code — zero callers anywhere
  (`grep` confirmed). Delete, or deliberate scaffolding for a not-yet-built feature?
- `vpnconnect/steps.go`'s `getClientKeyStep.Do` never returns an existing reusable VPN client key
  (reassigns its own field instead of writing through the pointer) — a bug the file's own comment
  says was carried over unchanged, on purpose, from the deleted `internal/pipelines`.
  `internal/jobs/connect_service_to_vpn.go`'s jobs-engine twin already fixes the identical bug.
  `vpnconnect` is still the live bootstrap-VPN path (not yet migrated to jobs). Backport the
  one-line fix now that it's proven safe, or leave until `vpnconnect` retires?
- `cluster_state/master.go`'s healthcheck-poll could nil-pointer-panic if a postgres container
  ever lacks a `HEALTHCHECK` (`.Health` would be `nil`); `enable_statefull.go`'s
  `waitForPgReadyJob` already guards the identical case and could be the fix pattern to port.
- This project's own CLAUDE.md Jobs-Engine rule #1 (oneof/JSON hazard) points at a file
  (`create_smerd_request_json.go`) that's since been deleted — the hazard is currently moot (no
  live `TaskPayload` embeds a oneof-bearing message, confirmed by reading every `*TaskPayload` in
  `api/grpc/tasks.proto`), but the rule text itself is still correct for the next time one is
  added. Recommend rewording it to note the historical fix is in, rather than pointing at a
  deleted file.
- All four of CLAUDE.md's jobs-engine hard-won rules re-verified as still respected in current
  code (DONE/FAILED invariant, scaffold-equivalence — moot, pipelines fully deleted).
- Open, not decided: whether `errors.Is`/`errors.As` on stdlib sentinels (`sql.ErrNoRows`, used
  repeatedly in the jobs engine core) should also go through `rerrors.Is`/`.As` for consistency —
  both forms already coexist in the codebase; purely stylistic since the pinned `rerrors` versions
  are themselves 1-line passthroughs.

Full detail: `docs/code-review-progress-part-cluster-jobs.md`.

---

## 4. `internal/service/` + `internal/storage/postgres/` — ✅ DONE

`go build ./...` clean; `golangci-lint run ./internal/service/... ./internal/storage/...` — 0
issues before and after; `go test` all green. `tests/e2e` not run.

**Fixed** (5, all naming/typo, zero behavior risk, verified via repo-wide grep for other callers):
`nodes_service.NewService`→`New`, `plugins.NewPluginService`→`New` (+ call-site updates in
`services.go`) — both packages export exactly one type, matching 5/7 sibling `service_manager/*`
packages already named plain `New`; `nodes_service/service.go`'s `rerrors` import aliased as
`errors` (misleading, unique in the repo) unaliased; a typo
`"erorr building list query"` → `"error building list query"` in `internal/storage/postgres/node.go`.

**Biggest finding — flagged, not fixed:** `31-go-layering.md` mandates `sql.Null[T]` for a missing
row ("Absorb `sql.ErrNoRows` inside the repo... service checks `if !result.Valid`, never
`errors.Is(err, sql.ErrNoRows)`"). The actual, **100%-consistent, repo-wide** pattern is the
opposite: `wrapPgErr()` (`storage.go:126`) translates `sql.ErrNoRows` → a typed
`storage.ErrNotFound` sentinel, checked everywhere via `errors.Is(err, storage.ErrNotFound)`. This
is exercised across all of `internal/service`, `internal/jobs`, `internal/transport` — not a
stray miss, the storage layer's one consistent contract. One layer of indirection *is* preserved
(nothing above the repo touches raw `sql.ErrNoRows` directly), it's just a sentinel-error shape
instead of `sql.Null[T]`. **Needs your call**: reword the rule to describe the sentinel pattern
Velez actually uses, or schedule the (large, cross-cutting) migration to `sql.Null[T]`.

**Other flagged, not fixed:**
- `SELECT *` / `RETURNING *` in 5 of 13 query files (`services.sql`, `registries.sql`, `tasks.sql`,
  `environments.sql`, `jobs.sql`) — violates 22-data-postgres's explicit-columns rule. The other 7
  query files are already fully compliant (reads as drift, not disagreement). Fixing means
  expanding to the real column list per table plus a `sqlc generate` regen — a real toolchain
  change, not a hand-edit.
- `ByID` vs `ById` casing inconsistency across 3 sqlc query names (which become Go func names) — a
  real `Id`-not-`ID` violation (30-go.md), but fixing means renaming + regenerating + multi-file
  call-site updates (`GetRegistryByID` alone has 3 call sites).
- No `service_manager/*` concrete type carries an `Impl` suffix (`Configurator`,
  `ContainerManager`, `VervService`, etc. instead) — systemic across all 7 sub-packages, reads as a
  considered pre-profile convention, not an oversight.
- `pgaas.CreatePgInstance`/`DropPgInstance` run 4–5 sequential cross-store writes with no
  `TxManager().Execute` wrapping — a real partial-failure risk, but `secrets.Store` can't
  structurally join a `*sql.Tx` (it may not even be Postgres-backed long-term), so "just wrap it"
  isn't mechanical — a real design call (accept the risk, add a compensating step, or split what's
  transactional more explicitly).
- At least 7 files in `verv_services` (+1 in `vervonomicon`) declare package-local `rerrors.New`
  error sentinels instead of routing through the existing `internal/user_errors` package — one of
  them (`ErrEnvironmentNotFound`) is already imported directly by transport tests, so relocating is
  a cross-package rename, not mechanical. Also: 30-go.md says the path is
  `internal/service/user_errors`; the real, apparently-deliberate path is one level up,
  `internal/user_errors` (transport needs it too, without importing through `service`) — likely a
  wording bug in the rule regardless of the sentinel-consolidation question.
- `deployments.go`'s `List` and `ListDeployments` are near-duplicate queries differing only in
  return shape — both genuinely used by different callers; collapsing them is a judgment call, not
  mechanical.

**Proposed rule changes** (4, awaiting sign-off — see consolidated list below).

Full detail: `docs/code-review-progress-part-service-storage.md`.

---

## 5. `internal/transport/` + `internal/workers/` + `tests/e2e/` (code-quality read-through) — ✅ DONE

`go build`/`golangci-lint run` on `internal/transport/...` and `internal/workers/...` — clean, 0
issues; `go test` all green. `tests/e2e/...` **not executed** this pass (a concurrent e2e
verification run was in progress against the same Docker fixtures — see §0 — running both risks
container-name collisions per this project's own "E2E shared-fixture rules"); read through for
code-quality/style only.

**Fixed** (2, empty-wrap-message smell, same class as `30-go.md`'s "wrap messages start with a
lowercase verb"): `vcn_api_impl/namespace_delete.go`'s `rerrors.Wrap(err, "")` → a real message;
`internal/workers/deploy_watcher.go`'s two identical sites
(`log.Error().Err(rerrors.Wrap(err, ""))...`) simplified to `log.Error().Err(err)...` since the
adjacent `.Msg(...)` already carried the context.

**Flagged, not fixed:**
- `velez_api_impl.NewImpl` should be `New` per `30-go.md` — the only one of six `*_api_impl`
  constructors that deviates; fixing touches DI wiring in `internal/app/custom.go`, out of
  no-brainer scope.
- The jobs-engine Enqueue → `context.WithTimeout` → Watch-loop-to-terminal-status pattern used by
  every jobs-engine-backed RPC handler (`velez_api_impl`, `internal/workers/deploy_watcher.go`) is
  heavier than `31-go-layering.md`'s literal "decode → call → encode, nothing else" — but it's
  consistent codebase-wide, reads as a deliberate architectural pattern, and probably deserves a
  documented exception in the profile rather than being treated as a defect.
- Raw `if err != nil { t.Errorf(...) }` (instead of `require`) in
  `vcn_api_impl/list_peers_test.go` and `service_api_impl/common_test.go` — flagged per this pass's
  conservative bar (test restructuring out of scope), same open naming question as Item K below.
- `internal/transport/http.go`'s `AllowAllOrigins` and `gateway_mux.go`'s cookie/CSRF wiring —
  cross-referenced only, already tracked as matreshka-migration scope creep in §0.

**e2e code-quality read-through** (26 files breadth-surveyed, 7 read in full): consistently high
quality — zero fakes/mocks anywhere (matches this project's real-API-only testing rule end to
end), shared-fixture singleton rules honored precisely as documented in this project's CLAUDE.md,
one deliberately-RED documented expected-to-fail test in `suite_container_runtime_test.go`
(intentional, not a leftover). No fixes needed.

Full detail: `docs/code-review-progress-part-transport-e2e.md`.

---

## Carried-over open items from `docs/dev-profile-coverage.md` (still unresolved)

| # | Item | Status |
|---|---|---|
| C | Cosmetic dupes in `.golangci.yaml` `enable:` list (`govet` x2, `dupword` x2) | Trivial — fix opportunistically |
| D | No linter for the *positive* snake_case-package preference | Review-only, not enforceable, leave as is |
| H | `rerrors.Wrap` message casing unenforced (custom-check candidate) | Root cause fixed in §0; the general custom-check idea still unbuilt |
| K | Test naming `Test_Thing_Scenario` (Velez convention) vs profile's `TestThing_Scenario` — surfaced again in §3/§4/§5's audits (transport, cluster, jobs test files all show a real 3-way mix: `Test_Thing_Scenario`, `TestThing_Scenario`, and no-underscore-at-all) | **Needs your call** — reword `32-go-testing.md` to match Velez, or treat as Velez-local override |

## Proposed rule changes awaiting your one-word approval

Nothing has been applied to `~/.claude/rules/` — every item below is a code-driven observation
from this run's audits, listed for your sign-off. One word each approves it.

1. **Enable `bodyclose` in `.golangci.yaml`.** (§2) Found a real, reachable HTTP response-body leak
   it exists to catch, plus a related double-close nearby.
2. **`31-go-layering.md` domain-purity rule** reads as aspirational, not descriptive: 6 of ~30
   `internal/domain/` files import proto or even a sqlc-generated storage type. (§2, cross-referenced
   in §4) Relax the rule for this project, or schedule the untangling as tracked debt.
3. **`31-go-layering.md` `sql.Null[T]` rule** vs. Velez's actual, 100%-consistent
   `storage.ErrNotFound`-sentinel pattern. (§4) Reword to describe the real pattern, or schedule a
   real migration.
4. **`30-go.md` constructor rule** needs a caveat: "always named `New`" assumes one exported
   constructible type per package; a package exporting more than one (`vervonomicon.NewBoxResolver`
   + `vervonomicon.NewImageSource`) names each `New<Type>`. (§4)
5. **`30-go.md` `Impl`-suffix rule** — every `service_manager/*` package uses a domain name
   (`Configurator`, `ContainerManager`, `VervService`) instead, with zero exceptions across 7
   packages. (§4) Reword to allow a domain name for a package's sole implementation, or record as a
   Velez-local override.
6. **`30-go.md` user-errors path** — rule says `internal/service/user_errors`; the real,
   apparently-deliberate path is `internal/user_errors` (one level up, since transport needs it too
   without importing through `service`). (§4) Reword to drop the `service/` segment.
7. **No `depguard` bans third-party error libraries** — one real instance
   (`github.com/pkg/errors` in `pull_image.go`) sat unnoticed since the earlier pass deleted the
   block as dead+stale rather than reconfiguring it. (§2) Worth a real re-add if this discipline
   matters going forward.
8. **`31-go-layering.md`'s literal "decode → call → encode, nothing else"** for transport handlers
   doesn't describe the jobs-engine Enqueue/Watch-loop pattern used consistently by every
   jobs-engine-backed RPC. (§5) Worth a documented exception rather than leaving handlers reading as
   rule violations.

---

## After Velez

Per your instruction: if this finishes and tokens remain, the same pass continues at
`~/verv/verv`, `~/ruf/zpotify`, `~/ruf/artel`. Logged in their own `docs/code-review-progress.md`
(or noted here if time runs out first).
