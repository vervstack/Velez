-- +goose Up
-- +goose StatementBegin

CREATE TABLE IF NOT EXISTS velez.registries
(
    id         INT8 GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name       TEXT        NOT NULL UNIQUE,
    type       TEXT        NOT NULL DEFAULT 'dockerhub' CHECK (type IN ('dockerhub', 'generic_v2')),
    url        TEXT        NOT NULL DEFAULT '',
    username   TEXT        NOT NULL DEFAULT '',
    secret     TEXT        NOT NULL DEFAULT '',
    is_default BOOLEAN     NOT NULL DEFAULT FALSE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

INSERT INTO velez.registries (name, type, is_default) VALUES ('Docker Hub', 'dockerhub', TRUE);

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin

DROP TABLE IF EXISTS velez.registries;

-- +goose StatementEnd
