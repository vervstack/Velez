# M3 — Observability

**Status:** Backlog — flesh out after M2 ships.

## Scope (draft)

- Container log streaming in the UI (tail / search)
- Per-container CPU and memory usage charts (polling or push)
- Deployment event timeline — a chronological feed of what changed and when
- Alert / threshold configuration for resource usage
- Export: download logs or metrics as CSV/JSON
