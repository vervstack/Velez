// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: runners.sql

package runners_queries

import (
	"context"

	"github.com/lib/pq"
)

const deleteRunner = `-- name: DeleteRunner :exec
DELETE FROM velez.runners
WHERE service_id = $1
`

func (q *Queries) DeleteRunner(ctx context.Context, serviceID int64) error {
	_, err := q.db.ExecContext(ctx, deleteRunner, serviceID)
	return err
}

const getRunnerByServiceID = `-- name: GetRunnerByServiceID :one
SELECT service_id, provider, scope, target, labels, secret_ref, created_at, updated_at, base_url
FROM velez.runners
WHERE service_id = $1
`

func (q *Queries) GetRunnerByServiceID(ctx context.Context, serviceID int64) (VelezRunner, error) {
	row := q.db.QueryRowContext(ctx, getRunnerByServiceID, serviceID)
	var i VelezRunner
	err := row.Scan(
		&i.ServiceID,
		&i.Provider,
		&i.Scope,
		&i.Target,
		pq.Array(&i.Labels),
		&i.SecretRef,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.BaseUrl,
	)
	return i, err
}

const listRunners = `-- name: ListRunners :many
SELECT service_id, provider, scope, target, labels, secret_ref, created_at, updated_at, base_url
FROM velez.runners
ORDER BY service_id
`

func (q *Queries) ListRunners(ctx context.Context) ([]VelezRunner, error) {
	rows, err := q.db.QueryContext(ctx, listRunners)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []VelezRunner{}
	for rows.Next() {
		var i VelezRunner
		if err := rows.Scan(
			&i.ServiceID,
			&i.Provider,
			&i.Scope,
			&i.Target,
			pq.Array(&i.Labels),
			&i.SecretRef,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.BaseUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertRunner = `-- name: UpsertRunner :one
INSERT INTO velez.runners (service_id, provider, scope, target, labels, secret_ref, base_url)
VALUES ($1, $2, $3, $4, $5, $6, $7)
ON CONFLICT (service_id) DO UPDATE
    SET provider   = EXCLUDED.provider,
        scope      = EXCLUDED.scope,
        target     = EXCLUDED.target,
        labels     = EXCLUDED.labels,
        secret_ref = EXCLUDED.secret_ref,
        base_url   = EXCLUDED.base_url,
        updated_at = NOW()
RETURNING service_id, provider, scope, target, labels, secret_ref, created_at, updated_at, base_url
`

type UpsertRunnerParams struct {
	ServiceID int64
	Provider  string
	Scope     string
	Target    string
	Labels    []string
	SecretRef string
	BaseUrl   string
}

func (q *Queries) UpsertRunner(ctx context.Context, arg UpsertRunnerParams) (VelezRunner, error) {
	row := q.db.QueryRowContext(ctx, upsertRunner,
		arg.ServiceID,
		arg.Provider,
		arg.Scope,
		arg.Target,
		pq.Array(arg.Labels),
		arg.SecretRef,
		arg.BaseUrl,
	)
	var i VelezRunner
	err := row.Scan(
		&i.ServiceID,
		&i.Provider,
		&i.Scope,
		&i.Target,
		pq.Array(&i.Labels),
		&i.SecretRef,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.BaseUrl,
	)
	return i, err
}
